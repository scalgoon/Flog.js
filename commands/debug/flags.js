

module.exports.run = async(client, message, args) => {
    const findFlag = args[0].match()
}

module.exports.config = {
    name: "debugflags", 
    aliases: ['df'],
    description: "Testing flags for systems",
}